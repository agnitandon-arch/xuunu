git add api/ vercel.json && git commit -m "Add Vercel API files" && git push origin main
git stash && git pull origin main && git stash pop && git add api/ vercel.json && git commit -m "Add Vercel API" && git push origin main